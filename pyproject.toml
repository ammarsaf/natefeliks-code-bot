[project]
name = "netflix-code-bot"
version = "0.1.0"
description = "Telegram bot to fetch Netflix codes from Gmail"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
]

[tool.uv]
dev-dependencies = [
    "workers-py",
]